  <%= simple_form_for [@watch, Booking.new] do |f| %>
    <%= f.input :booking_start_date, label: 'Date de début', as: :date, html5: true %>
    <%= f.input :booking_end_date, label: 'Date de fin', as: :date, html5: true  %>
    <%= f.submit "Demande de réservation", class: "btn btn-success btn-resa" %>
  <% end %>
