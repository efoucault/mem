<div class="container">
  <h1>Create a new booking</h1>
  <%= simple_form_for [@watch, @booking] do |f| %>
    <%= f.input :booking_start_date %>
    <%= f.input :booking_end_date %>
    <%= f.association : %>
    <%= f.submit "add a booking", class: "btn btn-success" %>
  <% end %>
</div>
