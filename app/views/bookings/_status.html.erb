  <% if current_user == @booking.watch.user %>
    <% if @booking.status == "20" %>
      <%= simple_form_for(@booking) do |f| %>
        <%= f.input :status, as: :hidden, input_html: {:value => "45"}%>
        <%= f.submit "Confirmer la location", class: "btn btn-success" %>
      <%end%>
    <% elsif @booking.status == "45" %>
      <%= simple_form_for(@booking) do |f| %>
        <%= f.input :status, as: :hidden, input_html: {:value => "100"}%>
        <%= f.submit "J'ai récupéré la montre", class: "btn btn-success" %>
      <%end%>
    <%end%>
  <%end%>

