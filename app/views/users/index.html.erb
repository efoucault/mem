<% @users.each do |user| %>
  <%= "#{user.first_name} #{user.last_name}. #{user.email} Address: #{user.address}. " %>
  <%= "#{user.description}" %> | <%= link_to 'Edit', edit_user_path(user) %></br>
<% end %>
